{
  "name": "manage-landing-page",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "author": "Qusz",
  "license": "MIT",
  "scripts": {
    "compile": "sass --no-source-map ./src/sass/main.scss:./src/main.css",
    "compile:watch": "sass --watch --no-source-map ./src/sass/main.scss:./src/main.css",
    "start": "wds --node-resolve --open '/src/'",
    "start:watch": "wds --node-resolve --watch --open '/src/'"
  },
  "dependencies": {
    "swiper": "^8.4.3"
  },
  "devDependencies": {
    "@web/dev-server": "^0.1.34",
    "sass": "^1.55.0"
  }
}
